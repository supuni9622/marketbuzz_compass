{
  "name": "marketbuzz-compass",
  "version": "0.1.0",
  "private": true,
  "description": "Internal revenue intelligence system for Clover app billing",
  "scripts": {
    "dev:api": "pnpm --filter @marketbuzz/api dev",
    "dev:web": "pnpm --filter @marketbuzz/web dev",
    "build": "pnpm -r build",
    "db:migrate": "pnpm --filter @marketbuzz/api db:migrate",
    "db:generate": "pnpm --filter @marketbuzz/api db:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=20"
  }
}
